-- ds.page:

-- ds.h1: Auth Provider Packages

Owner: AbrarK

We will convert each auth provider code, like github, telegram etc, into WASM
and will add provider by adding `-- fastn.dependency` instead of modifying `fastn`
codebase.

-- end: ds.page