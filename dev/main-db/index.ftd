-- ds.page: `main-db`
nav-title: Blog
nav-title-url: blog/
nav-sub-title: How to setup continuous documentation for your Github Repo
nav-sub-title-url: blog/setup/
login-button: true

We use `PostgreSQL` as our `main-db`. Currently this is the only database we
have.

This database is managed via `Django`, we use Django's DB migration feature to
update the database.

On prod we have our `main-db` is managed by Heroku.
