-- ds.page: `ftd`


We use [ftd_django](https://github.com/ftd-lang/python.ftd.dev/blob/main/django/template-backend)
as our template backend.

We use `fifthtry.github.io/cdoc` as fastn package where templates come from.
`cdoc-doc` is added as git-submodule in `cdoc-doc` folder in our repo.
